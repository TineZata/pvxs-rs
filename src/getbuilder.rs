pub struct GetBuilder {
    ptr: *mut std::ffi::c_void, // Pointer to the underlying C structure
    lib: libloading::Library, // Reference to dynamically loaded library
}

impl GetBuilder {
    
}

